when:
  event: [push, manual]
  path:
    include: ["*.rs", "*.toml"]
steps:
  test:
    image: rust:trixie
    commands:
      - apt update && apt install -y protobuf-compiler
      - cargo build --release --no-default-features --features libm
      - cargo test --release
